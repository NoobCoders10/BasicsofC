An assignment operator is used for assigning a value to a variable. The most common assignment operator is =
The assignment operator used for assign the value to the variable

Operator	  Example	 Same as
    =	      a = b	     a = b
    +=	      a += b     a = a+b
    -=	      a -= b	 a = a-b
    *=	      a *= b	 a = a*b
    /=	      a /= b	 a = a/b
    %=	      a %= b	 a = a%b

    I think we are discuss it on previous lectures. So I hope it is clear. So in further files we are going to write like this. 